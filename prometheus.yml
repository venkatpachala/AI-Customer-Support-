global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "d2c-agent"
    static_configs:
      - targets: ["host.docker.internal:8000"]   # Windows/Mac
        # use "localhost:8000" if running Prometheus without Docker